interface:
  display_name: Vibe Task Pack
  short_description: Generate task-level spec/map/handoff docs and update current-task.
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - bash
    - sed
    - grep
