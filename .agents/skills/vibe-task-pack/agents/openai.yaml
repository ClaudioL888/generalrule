interface:
  display_name: Vibe Task Pack
  short_description: 生成任务级 spec/map/handoff 文档并更新 current-task。
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - bash
    - sed
    - grep
