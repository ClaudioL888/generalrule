interface:
  display_name: Vibe Governance
  short_description: Run the task-level governance loop and connect the task pack with local gates.
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - bash
    - git
